spring:
  profiles:
    active: dev

---
spring:
  profiles: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vplan?serverTimezone=UTC&characterEncoding=utf-8
    username: root
    password:

server:
  port: 8080

mybatis:
  mapper-locations: classpath:xmlMapper/base/*.xml,classpath:mapper/*.xml





